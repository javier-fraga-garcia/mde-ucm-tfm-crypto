apiVersion: 1

datasources:
  - name: TimescaleDB
    uid: TimescaleDB
    type: postgres
    access: proxy
    url: timescaledb:5432
    database: serving
    user: serving
    secureJsonData:
      password: serving
    jsonData:
      sslmode: disable
      postgresVersion: 1600
      timescaledb: true
      database: serving
    isDefault: true
